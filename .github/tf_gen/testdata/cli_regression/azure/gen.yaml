providers:
  - provider_name: mongodbatlas
    resources:
      cloud_provider_access_setup:
        - output_dir: .
          label: this
          variables_excluded: [aws_config, gcp_config]
          outputs_excluded: [aws_config, gcp_config]
      cloud_provider_access_authorization:
        - output_dir: .
          label: this
          variables_excluded: [aws, gcp]
          outputs_excluded: [aws, gcp]
