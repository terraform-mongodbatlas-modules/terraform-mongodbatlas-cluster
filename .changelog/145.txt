```release-note:bug
module: Clamps electable `instance_size` into auto-scaling bounds (`<= compute_max_instance_size`, and `>= compute_min_instance_size` when scale-down is enabled) so an existing size outside the configured range does not fail the plan
```
